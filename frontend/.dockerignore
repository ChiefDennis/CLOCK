# Git and version control files
.git
.gitignore

# Node.js dependencies
# These are installed inside the container, so we don't need to copy them.
node_modules

# Build artifacts and compiled output
# These are generated inside the container during the build stage.
dist
build
.angular/cache

# IDE and editor configuration files
.vscode
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.swp

# Environment files
# These often contain secrets and should be handled separately.
.env
.env.*
!.env.example

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# The Dockerfile itself
Dockerfile
docker-compose.yml